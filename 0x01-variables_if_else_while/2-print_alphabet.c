#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - entery point
 *
 * Return: always (0) suces
 */
int main(void)
{
	int low;
	for (low = 97 ; low <= 122 ; low++)
		putchar(low);
		putchar('\n');
		return (0);
}
